import 'package:flutter/material.dart';

class WtvPage extends StatelessWidget {
  const WtvPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
